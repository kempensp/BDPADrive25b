<nav class="navbar">
  <div class="navbar-left">
    <a href="/">
      <img src="/images/bdpa-logo.png" alt="BDPA Logo" class="navbar-logo" />
      <span class="navbar-title">BDPADrive</span>
    </a>
  </div>
  <div class="navbar-center">
    <% if (user && user.username) { %>
      <form class="navbar-search" action="/search" method="get">
        <input type="text" name="q" placeholder="Search files, folders, tags..." value="<%= typeof searchQuery !== 'undefined' ? searchQuery : '' %>" required />
        <button type="submit">🔍</button>
      </form>
    <% } %>
  </div>
  <div class="navbar-right">
    <% if (user && user.username) { %>
      <a href="/dashboard" class="navbar-user"><%= user.username %></a>
      <a href="/auth/logout" class="navbar-logout">Logout</a>
    <% } else { %>
      <a href="/auth/login" class="navbar-login">Login</a>
      <a href="/auth/register" class="navbar-signup">Sign Up</a>
    <% } %>
  </div>
</nav>
